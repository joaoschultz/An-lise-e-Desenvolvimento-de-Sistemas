#include <stdio.h>      
#include <stdlib.h>     

int main() {          

    // Laço de repetição que será executado 5 vezes
    for (int index = 0; index < 5; index++) {
        // Exibe o valor atual da variável 'index'
        printf("O valor de Index é: %d\n", index);
    }
    return 0;           
}
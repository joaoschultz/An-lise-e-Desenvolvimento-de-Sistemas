#include <stdio.h>      
#include <stdlib.h>     

int main() {            
    int vec [5];
    int index = 0;

    index --;
    while (index >=0)
    {
        printf("O valor digitado para a posição %d do vetor foi: %d\n", index, vec[index]);
        index --;
            
}return 0;  
    }